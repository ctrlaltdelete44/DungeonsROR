<% if @account.errors.any? %>
    <h2 class='error-msg'>
        The form could not be saved due to the following <%= pluralize(@account.errors.count, "error") %>
    </h2>
    <% @account.errors.full_messages.each do |msg| %>
        <div class='error-box'>
            <p class='error-msg'><%= msg %></p>
         </div>
    <% end %>
<% end %>