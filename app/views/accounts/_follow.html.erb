<%= form_for(current_account.active_relationships.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :followed_id, @account.id %></div>
    <%= f.submit "Follow", class: 'subtle-submit' %>
<% end %>